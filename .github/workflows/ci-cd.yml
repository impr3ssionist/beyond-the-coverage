name: Environment Validation

on:
  push:
    branches:
      - develop
      - main
  pull_request:
    branches:
      - develop
      - main
  workflow_dispatch:

jobs:
  validate:
    name: Validate
    uses: ./.github/workflows/ci.yml
